var arr = [2, 4, 5, 7, 9, 4, 1, 16];

function getEven(arr) {
 let res = []
 for (let i = 0; i < arr.length; i++){
   if (arr[i] % 2 === 0) {
     res.push(arr[i])
   }
 }
 return console.log(res);
}

getEven(arr)
